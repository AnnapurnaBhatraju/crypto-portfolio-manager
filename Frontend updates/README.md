My react file setup show in "check.png". check.png is non-usable file.

simply put "Intro" file "js" and "css" both in src(source) file.
Intro file are extra files.

"LoginPage.js" is replacement of TestLoginPage.js of milestone 1.
12.png is extra file.

after that same put "loginPage.js" and "12.png" (image of LoginPage.js) in src file.

Same this is replacing file only you have to check over "App.js" and "App.css" these 2 file is little complicated only match the code , it little hard 😁.

also Simply add "StrategyMixer.js" in src(source) file, it still underconstruction add to word the app.js  

if these word we acheive milestone 1 easily.
