# Progress - Shivani

## Date: 21 Nov 2025

### ✔ MongoDB Setup Completed
- Created database user and gave access.
- Added IP Access List (current IP + Allow From Anywhere for now).
- Successfully connected MongoDB using pymongo driver.

### ✔ Password Security Setup
- Created `config.json` to store MongoDB username & password securely.
- Added `config.json` to `.gitignore` so credentials are hidden from GitHub.
- Updated `db_connect.py` to load credentials safely from JSON.

### ✔ Testing & Verification
- Ran `db_connect.py`.
- Connection successful and sample data inserted to MongoDB cluster.

### Pending:
- Continue documentation update.
